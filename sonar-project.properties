# Required metadata
sonar.projectKey=Docker-filr
sonar.projectName=Docker-filr
sonar.projectVersion=1.0

# Path to the source directories (comma-separated list)
sonar.sources=index.html

# Language of the source code
sonar.language=html

# Encoding of the source files
sonar.sourceEncoding=UTF-8

# Additional properties (optional)
# sonar.exclusions=**/*.xml  # Example exclusion pattern
# sonar.tests=test-src       # Path to test sources
# sonar.junit.reportPaths=test-results/junit/*.xml  # Path to JUnit test report
